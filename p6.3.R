categories <- c("A", "B", "C", "D", "E")
values <- c(10, 15, 7, 20, 12)
barplot(values, names.arg = categories, col = "lightblue",
        main = "Bar Chart Example", xlab = "Categories", ylab = "Values")
